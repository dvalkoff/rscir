version: '3.9'
services:
  server:
    build: .
    volumes:
      - ./src:/var/www/html
    ports:
      - "80:80"
  db:
    image: mysql
    volumes:
      - ./db/schema:/docker-entrypoint-initdb.d
    environment:
      MYSQL_ROOT_PASSWORD: password